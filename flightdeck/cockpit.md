# Cockpit — aep-parser

Focus: AEP 理解/复刻诊断/生成路线已开新 spec → `work/aep-understanding-generation/index.md`。Booyah Glitch 全工程复刻仍是当前压力测试基准 → `work/booyah-glitch-replication/index.md`。

## In flight

- **booyah-glitch-replication** (`work/booyah-glitch-replication/index.md`) — 全 12-comp from-scratch 复刻 ✅ 全建成 + 双版本 AE gated。①②③⑧ 已用户真机验收 complete；④⑤⑥⑦⑨⑩⑪⑫ 🔶待用户真机 review（agent 已 render 自验，按 showcase review-gate 用户验后才翻 complete + 关 plan）。逐 comp 账本（建法/值/delta/commit）= `showcase/INDEX.md`，进度 = `work/booyah-glitch-replication/index.md` → `plan.md`。
- **aep-understanding-generation** (`work/aep-understanding-generation/index.md`) — 新方向 spec：把 `aepdissect -json` / `Project.WriteJSON` / Booyah 渲染脚本 / 能力表收敛成 profile → diff → render oracle → gap ledger → recipe generation 的长期流水线。Phase 1 已抽 `internal/profile`，`aepdissect -json` 已切到 stable profile builder；自审补齐 track matte refs + parsed in/out points（2051e04）。Phase 2 已实现 `internal/profilediff` / `cmd/aepdiff` / JSON ignore rules（adfb7da）。Phase 3 已实现 `internal/aeoracle` / `cmd/aeoracle` / generic JSX render harness（053b3fc）。Phase 4 已实现 `internal/gapledger` / `cmd/aepgaps`；Phase 5 已实现 `internal/sliceworkflow` / `cmd/aepslices`，fixture + real Booyah source + non-Booyah Motionbox sample 验收通过（见 `phase5-booyah-acceptance.md` / `phase5-non-booyah-acceptance.md`），非 Booyah 样本已过 AE 2025 hard render gate。Phase 6 frame-set render compare 已实现并在 Booyah source-vs-clone 跑通；minimal recipe IR 已实现并通过 AE 2025 render gate；Technique Facts v1 已实现 `internal/technique` + `cmd/aeptechnique`，从 stable profile 派生 comp/layer/effect/text/shape/dependency/unknown JSON facts；Technique Portrait v1 已实现 `BuildPortrait` + `aeptechnique -mode portrait`，给 corpus 学习输出 fingerprint/signal/mechanism/graph/hint 画像；`aeptechnique -corpus -recursive` 已能对目录输出 facts/portrait JSONL，`go run ./cmd/aepselfhost technique-report` 生成 tmp 下 summary/corpus/report 三件套；recipe capability report 已接 `internal/capindex`；comp background_color/motion_blur(enabled + shutter/sample settings)/work_area/renderer/resolution_factor/pixel_aspect/display_start_time/frame_blending/hide_shy_layers/preserve_nested_frame_rate/preserve_nested_resolution/label/comment/motion_graphics_template_name + layer label/comment/motion_blur/shy/common+advanced switches/quality/blending/timing/parent/auto_orient/is_null/is_adjust/explicit AE2025 matte + camera.zoom/depth_of_field/focus_distance/aperture/blur_level/iris_shape/iris_rotation/iris_roundness/iris_aspect_ratio/iris_diffraction_fringe/iris_highlight_gain/iris_highlight_threshold/iris_highlight_saturation + light.intensity/light.color/light.casts_shadows/light.shadow_darkness/light.shadow_diffusion/light.falloff_type/light.falloff_start/light.falloff_distance/light.cone_angle/light.cone_feather/light.kind/light.source_layer + built-in effect + static/expression/scalar+vector keyframed/layer-reference effect params + static effect param EG controller + embedded expected-profile contract 已过 profile + AE 2025 render gate；solid/text/shape/null/adjustment/camera/light layer creation + text animator opacity/position/scale/rotation/color/tracking/character-offset/fill-opacity/stroke-opacity/stroke-width/skew/rotation-x/y/stroke-color/range-offset keyframes/scalar+vector value-keyframes + shape stroke/detail/style/dashes/taper/wave + fill blend mode + fill/stroke composite order + fill rule + gradient fill/highlight/alpha-stops + gradient stroke/style/highlight/alpha-stops + polystar + Trim Paths/Round Corners/Offset Paths/ZigZag/Pucker & Bloat/Twist/Wiggle Paths/Wiggle Transform/Repeater/Merge Paths filters + text style slices（font size/tracking/justification/fill/stroke/faux）+ `expected_profile.keyframes[]` Position/Anchor/Scale/Rotation/Opacity keyframe contract 已过 `go test ./...`、`go vet ./...` 和 AE 2025 render gate，并修正 `SetLayerTransform` stale boundary、加固 aeoracle PNG 输出校验（见 `phase6-recipe-ir-acceptance.md`）。
- **recipe-docgen** (`work/recipe-docgen/index.md`) — recipe IR 专属文档生成器已落地。`cmd/recipedocgen` 生成 `docs/recipe_schema.json` + `docs/recipe.md` + `docs/recipe_index.json`；字段结构来自 `internal/recipe` JSON tags，语义/约束/示例/能力 query 走 recipe-owned registry，再通过 `docs/capabilities.json` join capability 真相。当前已覆盖核心、shape、mask capability，expected_profile/effect validation，并从 `examples/recipes/*.json` 自动发现 425 个字段示例引用；`aeprecipe explain -field <path>` / `-search <term>` 可直接查询字段元数据。
- **project-index** (`work/project-index/index.md`) — 外部快照索引层已提供单项目 ID/source/effect/layer lookup，recipe parent/light-source lookup 与 profile `source_ref` 已接入；单项目 search API 已支持 source-layer/effect-match/property-match/expression-substring/text-font stable hits；corpus builder 已能流式抽取 layer-source/effect-usage/expression/text-style/shape-usage facts 且不保留项目图；`cmd/aepsearch` 已提供 search/corpus CLI 入口。
- **scene-architecture** (`work/scene-architecture/index.md`) — scene/serializer 架构清理线。breaking change 可接受，但只为长期结构收益；短期先用 composition source-view 局部索引避开 public slice 持久 index 失效风险，后续收拢 Layer 私有 runtime/writeback 状态，再机械拆 shape graph/lowering 超大文件，最后处理结构化 warnings 与事务回滚迁移。
- **cross-platform-architecture** (`work/cross-platform-architecture/index.md`) — 跨平台方向 spec：核心解析/profile/diff/recipe/search/report 必须 Pure Go 跑 Windows/macOS/Linux；公网服务默认不依赖 AE；selfhost 编排继续 Go 化；AE 验证/render/readback 变成可选 worker capability，Windows/macOS adapter，Linux 返回 unavailable。
- **apidoc-tag-schema** (`work/apidoc-tag-schema/index.md`) — @tag 文档 schema 已落地（facade 全转 + dual-read + jargon scrub COMPLETE）。仅剩**可选遗留** flip(c)：删 `extract.go` `parseCapTag` 旧读路径 + `tag.go` 旧枚举 map + 守卫测试 + `--validate` strict CI + 改文档真相源注脚 → regen → commit flip → done。dual-read 现仍工作、aep:cap=0，非阻塞，可随时做。
- **fx-technique-internalization** (`work/fx-technique-internalization/index.md`) — 上游「理解工程」研究 arc（暂让位）。通用机制：任何参考 .aep → 解析 → 拆角色 → 抽跨域技法原子 → 存两层结构（技法库 `knowledge/techniques/fx-techniques.md` + 现象配方）。火焰=实例#1已验证。下一步见 topic index。
- **technique-ontology** (`work/technique-ontology/index.md`) — 同上游 arc 的数据骨架：角色/技法/机制三轴本体 + schema v2。已经火焰/闪电/控制器三类压测；未验证大规模（N≫3）词表对齐。

## Next

- **versioned AEP migration**：`cmd/aepmigrate assess` + conservative `convert` 第一片已落地；convert 支持 no-layer comp retarget、默认 null/solid/adjustment/camera/light/text/empty-shape/parametric-graphic-filter-shape/precomp layer slices，并保留稳定 comp settings、renderer、MOGRT name、label/comment；静态 layer transform 已能从 profile 重建 Anchor/Position/Scale/Rotation/Opacity（keyframes 仍不声明支持），layer timing 已能重建 start/in/out/stretch；默认 solid 会用 profile-visible footage details 验证 source 尺寸/颜色，默认 text 用 `NewTextLayer`+`SetText` 并由 profile diff 校验 text document/style，shape 先支持 empty shape 与单 rect/ellipse/star-polystar fill/stroke/dashed-stroke/tapered-stroke/waved-stroke/trim-paths/round-corners/offset-paths/zigzag/pucker-bloat/twist/wiggle-paths/wiggle-transform/repeater/merge-paths（单组 Dash 1/Gap 1 + percent-mode taper + wavelength-mode wave + Polystar + Trim Paths + Round Corners + Offset Paths + ZigZag + Pucker & Bloat + Twist + Wiggle Paths + Wiggle Transform + Repeater + Merge Paths type；仍排除 stroke offset、多组 dash/gap、wave units/cycles、gradient/其他 filters），默认 precomp 会先重建 target comps 再解析 composition source refs；Go-writer 与 recipe 默认 null/solid/adjustment/camera/light/text/static-transform-text/layer-timing/empty-shape/rect-fill-shape/rect-stroke-shape/rect-dashed-stroke-shape/ellipse-tapered-stroke-shape/ellipse-waved-stroke-shape/trim-paths-shape/round-corners-shape/offset-paths-shape/zigzag-shape/pucker-bloat-shape/twist-shape/wiggle-paths-shape/wiggle-transform-shape/repeater-shape/merge-paths-shape/polystar-shape/precomp fixture 均已过 profile diff；成功输出会跑 source-vs-target profile diff gate，`-ae-open -ae <AfterFX.exe>` 可把 AE open gate 写进 report，且这些 recipe outputs 已过 AE2025 open smoke；unsupported layer 项目会 blocked，避免静默丢层。
- **aep-understanding-generation 下一步**：recipe IR 已能 one comp + comp background_color/motion_blur(enabled + shutter/sample settings)/work_area/renderer/resolution_factor/pixel_aspect/display_start_time/frame_blending/hide_shy_layers/preserve_nested_frame_rate/preserve_nested_resolution/label/comment/motion_graphics_template_name + layer label/comment/motion_blur/shy/common+advanced switches/quality/blending/timing/parent/auto_orient/is_null/is_adjust + camera.zoom/depth_of_field/focus_distance/aperture/blur_level/iris_shape/iris_rotation/iris_roundness/iris_aspect_ratio/iris_diffraction_fringe/iris_highlight_gain/iris_highlight_threshold/iris_highlight_saturation + light.intensity/light.color/light.casts_shadows/light.shadow_darkness/light.shadow_diffusion/light.falloff_type/light.falloff_start/light.falloff_distance/light.cone_angle/light.cone_feather/light.kind/light.source_layer + solid/text/shape/null/adjustment/camera/light + transform + supported built-in effect + static/expression/scalar+vector keyframed/layer-reference effect params + static effect param EG controller + shape stroke/detail/style/dashes/taper/wave/fill-blend-mode/fill-composite-order/stroke-composite-order/fill-rule/gradient-fill-highlight/gradient-fill-alpha-stops/gradient-stroke-style-highlight/gradient-stroke-alpha-stops/polystar + Trim Paths/Round Corners/Offset Paths/ZigZag/Pucker & Bloat/Twist/Wiggle Paths/Wiggle Transform/Repeater/Merge Paths + profile-visible text style + text animator opacity/position/scale/rotation/color/tracking/character-offset/fill-opacity/stroke-opacity/stroke-width/skew/rotation-x/y/stroke-color/range-offset keyframe/scalar+vector value-keyframe profile checks + Position/Anchor/Scale/Rotation/Opacity keyframe profile checks 编译 AEP，且 report 会列 used capabilities / downgrades / refusals / profile_checks。下一刀继续补更多 camera/light option setters、更多 comp settings、shape filters、表达式或 transform keyframe/ease（以 writer 支持为边界）；更多 text style 只补 profile 可读字段。不要开 automated correction loops。
- **用户真机验收 ⑩⑪⑫**（修后再验；按 review-gate 用户验后翻 complete + 关 booyah plan Task 4.3）。⚠AE 装 `E:\adobe\`，agent 自跑 `scripts/ae-worker/ae_run.ps1`。
- **booyah polish（非阻塞 documented delta）**：撕裂强度/文字色细调 · **eased Position kf**（LayerTransform 无 eased builder）· **Noise2 能力补**（不在 embed set）· **Curves 曲线数据**（arbitrary-data blocked）。
- **apidoc flip(c)** 可选遗留清理（见 In flight）。
- **旁支**：补 comp ① AE 2020 render；「gen 漏复刻层属性/标志」横切回扫——已撞两类活跳坑：①静态 Scale/Rotation/Anchor（`knowledge/layer/layer-replication-drops-static-transform-channels.md`）②motion blur 层标志（2026-06-22 frame26 揪出，已修 `SetMotionBlur`+`SetCompMotionBlur`）。

## Open questions

- **comp ①**：✅✅ AE 2025 render 与原工程像素级一致（中间帧 diff=0/0/32px，32px=sub-pixel 帧snap）。三个真 bug 全修：层 position→中心（4f579d1）· 层时长（6091016）· 中间帧错位根因=`deriveTickRate` 把 NTSC kf 时间读大 3×（根因档 `knowledge/composition/ntsc-tickrate-derive-3x-off.md`）。⚠AE 2020 侧 render 待补；clone 用 30fps 绕开 NewComposition 分数 fps cdta 时基 bug。
- **终帧保真审计（教训）**：复刻保真审计「运动曲线」必须同时查 kf interp + 表达式两个维度。kf ease 线程已查证证伪关闭（⑤⑥⑦⑧ Position/Opacity 全 394 kf 都 linear，唯一带 bezier ease 的是文字动画器 Tracking/CharOffset，② 早已拷）；但表达式维度揪出 ⑤L2/⑧ wiggle 漏复刻，已全扫 ⑤⑥⑦⑧ transform 表达式补齐。
- **复刻 = 理解压测定位**（用户校准 2026-06-21）：复刻不为产物，为逼出读侧漏解属性 / 写侧缺 API / 未知结构；终极=任给一个 .aep 能说清构成+内容+cover 边界。每 comp 先读侧全审计→列 gap→建可建的→诚实标缺口。
- **本会话沉淀**：库 fix `setLdtaFrac` 粗 divisor 截断（commit 2886c5e + 回归测试）· `knowledge/keyframe-expression/expression-enable-byte-pair.md`（kf+expr 坑扩到 effect param + 级联 drop，booyah ⑪ 实证）· `knowledge/shape/mask-shph-bbox-read-gap.md`（mask 真几何在 shph bbox，`Mask.Vertices` 只 surface 归一化 ldat）· Displacement Map tdpi = self-ref（`AddEffect` 默认绑宿主）。
- **AE 自动化基建**：`clear_ae_crashstate.ps1` tracked 工具（`tools/debug/`，67023b6）+ ae_run `Invoke-SendKeysSafe` 改 PostMessage 根治 foreground-lock（b411d09）。operator 在另一台机器、前台游戏窗口 ≠ 用户在用 → 不问用户让机器（`knowledge/workflow/ae-automation-occlusion-crashstate.md` Case 2c）。
- **pseudo label 红测已清**：`TestSynthControlEntries_PardLayout/label` 已按当前知识结论修正（普通 label @0x04=0，Dimmed label @0x04=0x20）；`go test ./...` 绿。
- **能力真相源** = `go run ./cmd/capindex -q <词>`（不再维护单独 coverage 表）。
