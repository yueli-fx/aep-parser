# Logbook — aep-parser

## Recently finished

- **2026-05-30 `internal/aep` package 重组 landed — 方案① 命名轴收口 + AST 边界守卫，零行为/零 API 变更**（autonomous subagent-driven：brainstorm→spec→plan→13 任务→merge 一程，21 commits 直上 main）。`<stage>_<domain>` 7 前缀（scene_/codec_/parse_/lower_/write_/back_/mutate_）取代旧三套正交轴；拆 `types_core`(1043→scene_{project,composition,layer,property}) + `scene_layer_accessors`(946→2)；5 个 test 巨文件按 feature 拆 + helper 归并 `testutil_*`；新增常驻 `arch_boundary_test.go`（AST：scene_ 禁 import rifx + codec_ 禁 scene 类型；scene→rifx 残留 5 项白名单 → V3 M8 清零，见 v3-direction spec）。**关键判断**（spec §0）：物理分包被 Go「方法与类型同包 + Stable API + 循环依赖」挡死 → 收敛为单包命名轴，back_/codec_ 预先固化为 方案② 断层线（"make the change easy, then make the easy change"）。**三重验证全绿**：每步 byte-identical round-trip(115 fixture sha256 基线) + exported-API 集合不变(sorted go-doc，base-worktree 对比) + AE 双版本 ship-gate 24/24 PASS。Gate 自身踩 3 坑修正：`go doc -all` 内嵌源文件名行 + 改名重排声明顺序/空行 → 指纹须 filter+sort 做集合比对。两轮外审(ds/claude/gpt)Approved。坑：多文件 git-mv 期间 gopls 报 phantom duplicate（`go build` 才是权威）；AE ship-gate cold-start 撞崩溃恢复 modal（clear_ae_crashstate 清不掉 → 手动清 AE 后 warm retry 即过）。Next：233 处 comments.md §3 预存注释债另开分支清。
- **2026-05-29 V2.2.1 ShapeLayer 拓展全部完 — 全 5 shape kind + 3 潜伏 bug + 3 类 keyframe，12 个 AE 2020+2025 ship-gate PASS**（autonomous，一程从 Ellipse 推到 keyframe，~22 commits 直上 main）。(1) **5 shape kind embed**：Rect/Fill（既有）+ Ellipse/Stroke（embed+overwrite cdat）+ Path（embed scaffolding + splice `encodeBezier` 几何）。每个 AE-native fixture → `extract_shape_bodies` → embed → 聚焦 assert-based ship-gate（re-save + 解析器读回）。(2) **3 个潜伏 bug**（皆因从未真 ship-gate 而隐藏）：**AE 2020 ldta 地基**（`buildLdtaBytes` 硬编码 164B，AE 2020 判所有 shape 层损坏跳过；改 capability matrix `LdtaSize` 160/164 by target，incident `ae2020-shape-ldta-164-corrupt.md`）；**encodeBezier ldat 顶点编码**（实为 `[anchor, anchor+out_i, anchor_{i+1}+in_{i+1}]` wrap mod n）；**Fill/Stroke 颜色 ARGB×255**（`encodeShapeColorBE`，解 deferred 老谜）。(3) **keyframe 持久化** `injectAnimatedStream`（static cdat ↔ animated `LIST(list)(lhd3+ldat)` + tdb4 标志 patch）：Rect/Ellipse Size（non-spatial bpk 88）、Fill/Stroke Color（spatial dim4 bpk 152）、Ellipse Position（spatial motion-path bpk 104，`valueLayout.motionPath`）。ldat 多与 AE 字节一致。(4) 清死代码（nodeBodyTdgp/emptySubPropPlaceholder）。坑：`rifx.IDTdb4` 大写 legacy vs 小写 `tdb4`；AE 自动化遮挡/崩溃级联见 incident `ae-automation-occlusion-crashstate.md`。剩 **Layr Position keyframe**（bpk 128 + transform-group 路径）+ Path keyframe（V2.3+）+ 次要子属性 deferred。
- **2026-05-29 V3 Phase 5D DuplicateComposition 完 — Stable, 2/2 AE 2020+2025 ship-gate PASS**（autonomous，承接 InsertLayer 后顺势做完 Phase 5 三选一里的 #1 DuplicateItem）。全流程一夜走完 RE→design→TDD→ship-gate→Stable：(1) **RE** `re_duplicate_item.jsx` 探 AE `CompItem.duplicate()` 字节行为（AE 2020）——新 comp item ID（idta @0x10）、每层新 layer ID（ldta @0x00）、**intra-comp parent/matte ref 被 REMAP 到 dup 自己的层**（src `[2].parent=31`→dup `[2].parent=36`，crux）、source SHARED 不复制（compC/footage item ID verbatim）。FootageItem/FolderItem 无 `.duplicate()` ScriptingAPI → 本期只 scope comp，故 API 命名 `Project.DuplicateComposition`（非 cockpit 设想的泛型 `DuplicateItem`）。(2) **design** `specs/2026-05-29-v3-phase5d-duplicatecomposition-design.md`（含 RE 证据 + 算法 + refuse 矩阵 + ship-gate 计划）。(3) **impl** `internal/aep/duplicate_composition.go` —— deepClone Item LIST + 8 sibling run、alloc comp ID、两-pass remap（pass A 收 srcID→dupID map 并改 ldta @0x00，pass B 改 @0x84 parent + @0xA0 matte 经 map）、Utf8 name length-variable rewrite、rootFold splice + parseComposition reparse + 四元组 snapshot warnings-rollback。新机制 vs InsertLayer = remap（InsertLayer 是 zero-reset，因为跨 comp 孤儿；DuplicateComposition 是同 comp 复制故 ref 要重指 dup 自己）。6 Go test PASS（refuse nil/empty-name/cross-Project + happy-path parent-remap + round-trip + fresh-Data-slices）。(4) **ship-gate** `tmp_debug/ge_duplicate_composition` + `verify_ge_duplicate_composition.jsx`（per-version `.done` tag）：AE 打开 Go-emit 文件，确认 `A_solid parent=A_precomp@compA_dup`（**AE 自己确认 parent 落在 dup 内**）+ source 共享。AE 2020(17.7) + AE 2025(25.1) = 2/2 PASS（AE 2020 首跑撞 cold-start splash 假阴性 exit 2，warm retry exit 0）。godoc Stable + coverage row。**解锁 Phase 5C.1 cross-Project InsertLayer**（现 R7 refuse 的临时方案可换成 DuplicateComposition + same-Project InsertLayer）。坑：`re_duplicate_item_before.aep` baseline 反复撞 AE 2020 cold-start splash 生不出来（ship-gate 改用 `_after.aep` 作 ge 输入，里面含 compA_main 够用）。
- **2026-05-29 V3 Phase 5C InsertLayer 完 — Alpha→Stable, 6/6 AE 2020+2025 ship-gate PASS** — Go-side（refuse R1-R11 + happy-path × 3 splice + round-trip + concurrent-mutate）此前已落，本次补完 ship-gate。三件事：(1) `re_insert_layer.jsx` footage mode 修 bug —— `app.project.items.addSolid` 不存在（addSolid 只在 LayerCollection），改成 `compA.layers.addSolid` 后 `footageF1 = src.source` 抓共享 footage item（commit 646f9ed）。AE 2020 重跑出 6 个 baseline（`re_insert_layer_{basic,footage,precomp}_{before,after}.aep`，gitignored）。(2) `tmp_debug/ge_insert_layer/main.go` —— open 各 `_before` baseline（precomp 有 3 comps 故按 **name** 找 compA_src_holder/compB_dest 非 index）→ `compB.InsertLayer(compA.Layers[0], 0)` → emit `ge_insert_layer_<mode>.aep`。**Go-emitted 字节大小与 AE copyToComp `_after` baseline 完全一致**（118455/126439/176361）。(3) `verify_ge_insert_layer.jsx` —— AE open Go 文件无损坏 + compB.numLayers 增 1 + clone 在顶 idx 1 无 parent 无 matte + `clone.source.id == compA src.source.id`（SourceID verbatim）。坑：verify JSX 初版 `.done` 文件名只带 mode，但 ae_run.ps1 等带 version-tag 的名 → 每个空等 240s 超时报 exit=1（实际验证早跑通）；加 `GE_INSERT_TAG` env 让同 mode 跨版本写不同文件解决。6/6 PASS (AE 17.7x45 + 25.1x68)。godoc ALPHA→Stable + coverage row（commit 后续）。Next: Phase 5 三选一（DuplicateItem / V2.2.1 ShapeLayer 拓展 / Phase 5C.1 cross-Project InsertLayer）需用户定方向。
- **2026-05-29 V3 Phase 5C InsertLayer 实施 plan 落地** — `flight-plans/2026-05-29-v3-phase5c-insertlayer-plan.md` (4 phase × 10 task TDD 分解)：Phase A R1-R11 refuse 三任务 / Phase B happy-path 实现 + splice 位置 + safety + round-trip 四任务 / Phase C `re_insert_layer.jsx` 3 modes × 2 states 用户跑 / Phase D coverage + Alpha→Stable godoc 收尾。Plan-level deltas vs spec d5ba860：(1) fixture 路径 `test_data/项目/` → `test_data/` (flat dir 实际惯例)；(2) spec invented `findFirstLayerSlot` → 复用 existing `internal/aep/new_layer.go:88 insertLayrPosition`。B.2 step 1 含完整 InsertLayer 实现可直接落（无 placeholder）：snapshot 四元组 + 4 byte writes 含 `len(ldta) >= 0xA4` 守 + 3-branch splice (空 dest / 中部 / 尾部 append) + parseLayer 后 warnings-as-failure rollback。下一步用户选执行路径。
  〔更早条目（Phase 5C spec/impl、Phase 5B explicit-matte、Phase 5A、Phase 4 Move*）见 git log〕

## Deferred

- **V2.2.1 剩余 keyframe / 子属性** — Layr Position keyframe（bpk 128 spatial 变体 + transform-group 路径，RE 已起）、Path keyframe（V2.3+）、各 shape 次要子属性（Fill/Stroke Opacity·BlendMode·CompositeOrder、Stroke Line Cap/Join/Miter/Dashes/Taper/Wave、Direction、Layr Transform Anchor/Scale/Rotation/Opacity，多 runtime-only）。详 coverage.md 子项④。
- **V3 capability framework** — Layer.Remove/Duplicate/Move/PropertyBase 结构性 ops 都靠这套；进 Phase 3
- `environmentLayer` 360° 素材 / `ligature` OT liga 字体 / `maskFeatherFalloff` 位置未 RE
- Composition.SetRenderer / ldta 零值区 probe / Footage proxy / Project nhed 扩展 — 见 [coverage.md](flight-plans/coverage.md) "剩余可探方向"
- **`linearizeWorkingSpace` ScriptingAPI quirk** — chunk byte 跟 AE 自己写一致但 ScriptingAPI 读不到 true，归 OCIO/CMS-联动；详 [incident-reports/project-flag-chunks-lnrb-lnrp.md](incident-reports/project-flag-chunks-lnrb-lnrp.md)
