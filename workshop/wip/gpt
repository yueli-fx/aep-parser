这个验证价值非常高。
现在很多东西终于“塌缩”成一个明确问题了。

你已经正式确认：

importer pipeline 分成两阶段
阶段 1：binary parse

你的文件已经 PASS。

说明：

chunk framing
offsets
stream integrity
parser-level invariants

基本没问题。

阶段 2：semantic object materialization

这里失败了。

也就是：

Layr chunk exists
BUT
AE refuses to instantiate/register it as a comp layer

这和之前“怀疑 parser corruption”已经完全不是一个问题域了。

而且 tolerance 的结果还给了一个超关键事实：

class=ShapeLayer

这不是普通 metadata。

这是：

AE 内部最终实例化类型

说明 importer 最终会做：

binary object
→ semantic layer subtype resolution
→ instantiate ShapeLayer
→ attach to comp.layers

而你的文件：

很可能卡在：

binary Layr
→ subtype resolution FAILED

于是：

object 被丢弃
comp.layers 不注册
无 warning
project 仍 open success

这和很多 Adobe legacy importer 的行为模式完全一致。

你下一步应该怎么 RE

现在最该做的：

不是继续 patch。

而是：

建立 “layer admission minimal model”

也就是：

一个 Layer 被 AE 接纳进 comp.layers，
最少需要哪些 semantic links / type tags？

我会优先查 4 件事
1. tolerance 的 ShapeLayer 到底有什么“身份字段”

不要再看：

duration
ticks
tdum
tduM

这些现在大概率只是 payload。

你现在该看：

这个 object 为什么最终变成 ShapeLayer？

重点找：

type discriminator
class id
subtype token
match name
plugin id
stream type
2. Comp ↔ Layer registry linkage

重点不是：
“Layr chunk 在不在”。

而是：

Comp 如何引用 layer？

尤其：

layer count 来源
layer table
child object refs
ordered layer IDs

很多老格式会：

Comp contains layer reference table

而不是：
“扫描所有 Layr chunk”。

如果 registry 没挂：
AE 就直接忽略。

这是我目前最高概率 hypothesis。

3. ShapeLayer vs generic AVLayer 差异

你现在需要知道：

Nested 为什么是 ShapeLayer？

因为：

如果 AE importer：

只接受某些 subtype
或默认 generic layer 非法

那你现在其实一直在造：

syntactically valid
semantically incomplete
generic layer shell

AE parse 但拒绝 instantiate。

4. object graph completeness

我越来越怀疑：

你缺的是：

Layer
↔ Source
↔ Stream
↔ PropertyGroup

中的某个 mandatory semantic node。

也就是说：

不是字段错，
而是 graph 不闭合。

Adobe 老格式非常爱这种设计。

你现在应该避免什么

避免再做：

“看到一个不同字段 → patch → reopen”

因为：

你已经证明：

local field parity ≠ semantic equivalence

这是 huge milestone。

很多 RE 项目会卡很久才意识到这点。

我建议你现在建立一个 RE 文档

不是 diff log。

而是：

semantic map

例如：

## CompItem admission path
- comp.layers.length 来源？
- layer refs 在哪？
- object ownership 如何表达？

## Layer subtype resolution
- ShapeLayer discriminator?
- generic AVLayer possibility?
- required property groups?

## Object graph
Comp
 └── LayerRef
      └── Layer
           └── StreamGroup
                └── ShapeRoot

你现在已经进入：
“理解 runtime object model”
而不是：
“修 binary corruption”。

这是阶段转换点。