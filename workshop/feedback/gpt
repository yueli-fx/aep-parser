整体方向是成立的，且这是目前文档里最“V3-ready”的部分。它真正把：

runtime semantic
serializer artifact
bootstrap substrate
capability candidate
negative finding

五类概念拆开了。

这一步比具体 API 更重要，因为它决定未来 V3 不会再次退化成 “parser struct = runtime model”。

几个关键点评估：

1. 6.1 分类框架：正确，而且是长期资产

这是整个 V2.2 文档里最有价值的部分之一。

尤其这条：

runtime semantic 必须跨 AE 版本含义一致

非常关键。

它天然排除了：

chunk ordering
offsets
match-name
padding
hidden counters
tdgp child count
AE rewrite quirks

这些东西进入 runtime。

这实际上是在给 V3 scene IR 立法。

建议：

把这一句提升为正式 invariant：

Inv-X:
Runtime semantic MUST NOT depend on:
- chunk ordering
- chunk IDs
- byte offsets
- match-name strings
- AE version-specific serialization behavior

这会成为未来 reviewer 的判断标准。

2. 6.2 Runtime semantic catalog：已经接近 V3 IR registry

这里已经不是“V2.2 spec”。

实际上是：

scene graph primitive registry
animation primitive registry

尤其：

PropertyStream[T]
PropertyGroup
BezierPath
ShapeNodeKind
Time domain = seconds

这些已经是 V3 runtime core。

这是好事。

说明 V2.2 没在继续“patch-based thinking”。

3. PropertyGroup 的 runtime-name 决策是对的

这是 Section 6 最正确的设计之一。

你已经明确：

runtime name stable
match-name serializer-only

这非常重要。

否则 AE match-name 会污染整个 public API。

例如：

"ADBE Vector Rect Size"

根本不该进入用户世界。

runtime-facing：

Stream("Size")

才对。

这会让未来：

Resolve()
expression binding
graph traversal
UI abstraction

都更干净。

4. 6.3 Serialization artifact catalog：owner mapping 非常重要

这里真正有价值的不是 artifact 本身。

而是：

artifact -> lowering primitive owner

例如：

BezierPath ldat encoding
  owner = lower_property_stream.go

这意味着：

未来 serializer 重写知道责任边界
bug 知道归谁
capability branch 知道放哪
V3 migration 知道迁哪个包

这是 architecture mapping。

非常对。

5. “Chunk ordering rules = implementation-defined except AE compatibility”

这是成熟判断。

不要把：

exact ordering

升级成 semantic contract。

否则未来 serializer 永远不能重构。

你现在定义的是：

AE-compatible ordering

而不是：

historical byte identity

这非常健康。

6. 6.4 substrate 分类：已经明显优于“template-copy”

你已经完成了最关键的认知转变：

template != semantic object

dummy_comp.aep：

是 bootstrap substrate
不是 runtime archetype
不是 serializer truth

这是正确的。

否则未来：

1000 effects
10 layer kinds
nested shape systems

都会爆炸。

7. 6.5 capability candidate：现在这个状态是健康的

这里最成熟的一点：

记录 candidate
但不提前 admission

这是正确的。

否则 V2.2 会重新陷入：

if AE2020
if AE2025

地狱。

现在：

capability matrix 概念存在
serializer 有入口
但不提前复杂化

这是最佳平衡。

8. “AECapabilities 大概率仍空 struct” 是好信号

这实际上意味着：

minimum canonical subset strategy works

这是 V2.1 最大验证成果之一。

如果 V2.2 还能继续：

单 canonical lowering
AE auto-fill defaults
cross-version readable

那 V3 serializer 会轻很多。

9. 6.6 Negative findings：必须保留

这是很多 RE 项目最缺的东西。

因为：

quirk != capability
quirk != runtime semantic

例如：

NTSC shutter ×1.2

这是：

AE scripting bug/quirk
不是 runtime truth
不是 serializer invariant

分类正确。

否则 runtime model 会越来越脏。

10. 6.6a Decision log：强烈建议保留

这个非常好。

因为未来最容易丢失的是：

“为什么当时归到 serialization，而不是 runtime？”

decision log 能保住 reasoning。

尤其对 V3 很重要。

11. 6.7 inheritance map：这是 V3 onboarding 文档

这一段其实已经：

不是 V2.2 内容
而是 V3 migration index

非常有价值。

尤其：

serialization artifacts -> owner mapping

未来迁 serializer 包时会极其有用。

12. 一个建议：增加 “Unclassified / pending” 临时区

RE 阶段一定会遇到：

不知道属于 runtime 还是 serialization

建议允许：

[unclassified]

避免实施期间为了推进而错误归类。

等 RE 完成再 finalize。

否则会出现：

premature semanticization
accidental serializer leakage
13. Section 6 实际意义

你现在做的已经不是：

“支持 ShapeLayer”

而是在建立：

AE runtime ontology

也就是：

哪些概念是真实 runtime object
哪些只是 wire format
哪些是 bootstrap bytes
哪些是 version capability
哪些只是 AE 怪异行为

这正是 V3 需要的基础。

结论：

Section 6 是 OK 的，而且可能是整个 V2.2 spec 最重要的部分。

它真正解决了：

parser struct ≠ runtime model

这个 V2.1 暴露出的核心架构债。