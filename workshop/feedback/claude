Section 6 是全文最有长期价值的部分，classification framework 本身设计得很好。反馈较少，主要是几个完整性问题。

需要修改的地方
6.5 admission rule 的"三条件"没有在 Section 6 内定义
多处写"走 Section 1 admission rule 三条件检查"，但 Section 6 是独立阅读时的封口文档。V3 brainstorm 阶段的人直接看 6.5 时不知道三条件是什么。建议在 6.5 顶部加一个 inline 引用框：

Admission rule（定义在 Section 1）：(1) 跨版本有实测差异 (2) escape hatch 单一 canonical 不能覆盖 (3) lowering 必须 branch。三条同时满足才入 capability matrix。

不需要重复全文，一句话锚点就够。

需要补充的地方
6.2 缺少两个 V2.2 明确引入的 runtime concept
对照 Section 3/4，以下两个概念在 6.2 catalog 里没有出现：

StreamMode enum (Static / Animated) — 已在 catalog 末尾，实际有，只是我看漏了。但 KeyframeBuilder 没有出现。即使 Section 3.4 建议不用 Builder，如果最终 ship 了这个类型，它需要归类（runtime semantic 还是 internal implementation detail？建议明确标注为 internal，不入 runtime catalog）
Layer Position 的 2D/3D 维度 — 这是 Section 5 的 OQ，但如果 V2.2 定为 2D，layer.Position() 返回 *PropertyStream[[2]float64] 这个维度决策本身应该在 6.2 里标注，因为它是跨 AE 版本的 runtime semantic（AE 内部 3D layer 和 2D layer 的 Position stream 维度不同）

6.6 PathNode 最少顶点数量没有确定
6.6 写了"AE drops 0-vertex / 1-vertex Path"，处理是"SetVertices 校 len >= 2"。但 Section 3.3 留的 OQ 是"最少几个顶点"，这里给出了答案（>= 2）但没有标注这是 RE-S8 确认的还是推测。如果是推测，应该标注"待 RE-S8 确认"；如果是确认的，应该移到 6.2 的 PathNode 条目里作为 runtime invariant。

小问题

6.3 表格里 "Chunk ordering rules (S-Inv-2)" 的 "S-Inv-2" 前缀不一致——其他地方写的是 "Inv-2"，这里多了 "S-"，建议统一
6.7 "V3 可换格式（e.g. byte const → external .aep）" 这句话值得多说一句：byte const 嵌在 Go 源码里是 V2 的 bootstrap 策略，V3 换成外部文件是为了 RE 友好。这个方向判断正确，但如果不写原因，V3 的人可能不明白为什么要改
6.8 维护节奏合理，但没有说明谁负责维护。建议加一句"每个 Phase 的 subagent 在 commit 前负责更新对应段"，否则容易漂移


总结
Section 6 作为封口文档是合格的，classification framework 是 V3 真正能用的输入。两个需要在 Phase 1 RE 前处理的点：admission rule 三条件 inline 引用，以及 PathNode 最少顶点的 RE vs 推测标注。其余可以在 Phase 6 commit 时一起填。