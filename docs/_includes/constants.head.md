<!-- Hand-authored lead. The enum sections below are generated from each type's
     doc comments + constant blocks in internal/aep. -->

This page collects the package's enums and constant sets in one place. Each
enum is also documented on its home object's page; this is the cross-cutting
reference.
